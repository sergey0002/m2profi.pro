# Deny all HTTP access to converter tooling (scripts, reports, deps).
# Apache 2.4+
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
# Apache 2.2
<IfModule !mod_authz_core.c>
	Order deny,allow
	Deny from all
</IfModule>
Options -Indexes
